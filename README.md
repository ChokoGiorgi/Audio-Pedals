# Audio-Pedals
A digital pedalboard with the engine written in Rust.

# Usage
```bash
cargo run
```

if you want to change the input file modify `main.rs` and change the path to the file you want to use. The output will be written to `output.wav` in the same directory as the input file.